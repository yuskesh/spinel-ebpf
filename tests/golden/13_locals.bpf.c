// SPDX-License-Identifier: GPL-2.0 OR MIT
//
// GENERATED by spinel-ebpf. Do not edit by hand.
// Source unit: 13_locals.rb
// ebpf-eligible methods: 3, classes touched: 0

#include "vmlinux.h"
#include <bpf/bpf_helpers.h>

char LICENSE[] SEC("license") = "Dual MIT/GPL";

/* ctx for calc — userspace fills before bpf_prog_test_run */
struct calc_ctx {
    __s64 a;
    __s64 b;
};

/* ctx for step — userspace fills before bpf_prog_test_run */
struct step_ctx {
    __s64 n;
};

/* ctx for reassign — userspace fills before bpf_prog_test_run */
struct reassign_ctx {
    __s64 n;
};

/* impl: calc : int  params: a: int, b: int */
static __noinline __s64 calc_inner(__s64 a, __s64 b)
{
    __s64 x = 0;
    __s64 y = 0;
    x = a + b;
    y = x * 2;
    return y - 1;
}

/* entry wrapper: calc */
SEC("syscall")
int calc(struct calc_ctx *ctx)
{
    return (int)calc_inner(ctx->a, ctx->b);
}

/* impl: step : int  params: n: int */
static __noinline __s64 step_inner(__s64 n)
{
    __s64 inc = 0;
    __s64 doubled = 0;
    inc = n + 1;
    doubled = inc * 2;
    return doubled;
}

/* entry wrapper: step */
SEC("syscall")
int step(struct step_ctx *ctx)
{
    return (int)step_inner(ctx->n);
}

/* impl: reassign : int  params: n: int */
static __noinline __s64 reassign_inner(__s64 n)
{
    __s64 acc = 0;
    acc = 0;
    acc = acc + n;
    acc = acc + n;
    return acc;
}

/* entry wrapper: reassign */
SEC("syscall")
int reassign(struct reassign_ctx *ctx)
{
    return (int)reassign_inner(ctx->n);
}
