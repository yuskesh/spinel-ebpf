// SPDX-License-Identifier: GPL-2.0 OR MIT
//
// GENERATED by spinel-ebpf. Do not edit by hand.
// Source unit: 19_emit_str.rb
// ebpf-eligible methods: 1, classes touched: 0

#include "vmlinux.h"
#include <bpf/bpf_helpers.h>
#include "spnl/types.h"
char LICENSE[] SEC("license") = "Dual MIT/GPL";

/* === per-unit int-event channel === */
struct u_19_emit_str_event {
    struct spnl_event_hdr hdr;
    __s64 value;
};

struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 256 * 1024);
} u_19_emit_str_events SEC(".maps");

/* === per-unit string-event channel === */
struct u_19_emit_str_str_event {
    struct spnl_event_hdr hdr;
    char str[256];
};

struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 256 * 1024);
} u_19_emit_str_str_events SEC(".maps");

/* ctx for tracepoint__syscalls__sys_enter_openat — userspace fills before bpf_prog_test_run */
struct tracepoint__syscalls__sys_enter_openat_ctx {
    __s64 dfd;
    __s64 filename;
};

/* impl: tracepoint__syscalls__sys_enter_openat : int  params: dfd: int, filename: int */
static __noinline __s64 tracepoint__syscalls__sys_enter_openat_inner(__s64 dfd, __s64 filename)
{
    {
        struct u_19_emit_str_event *_e1 = bpf_ringbuf_reserve(&u_19_emit_str_events, sizeof(*_e1), 0);
        if (_e1) {
            _e1->hdr.type = SPNL_EVT_USER_BASE;
            _e1->hdr.version = SPNL_EVENT_HDR_VERSION;
            _e1->hdr.reserved = 0;
            _e1->hdr.timestamp = bpf_ktime_get_ns();
            _e1->value = dfd;
            bpf_ringbuf_submit(_e1, 0);
        }
    }
    {
        struct u_19_emit_str_str_event *_se2 = bpf_ringbuf_reserve(&u_19_emit_str_str_events, sizeof(*_se2), 0);
        if (_se2) {
            _se2->hdr.type = SPNL_EVT_USER_BASE;
            _se2->hdr.version = SPNL_EVENT_HDR_VERSION;
            _se2->hdr.reserved = 0;
            _se2->hdr.timestamp = bpf_ktime_get_ns();
            bpf_probe_read_user_str(_se2->str, sizeof(_se2->str), (const void *)(filename));
            bpf_ringbuf_submit(_se2, 0);
        }
    }
    return 0;
}

/* entry wrapper: tracepoint__syscalls__sys_enter_openat [tracepoint -> tracepoint/syscalls/sys_enter_openat] */
SEC("tracepoint/syscalls/sys_enter_openat")
int tracepoint__syscalls__sys_enter_openat(void *ctx)
{
    (void)ctx;
    (void)tracepoint__syscalls__sys_enter_openat_inner((__s64)((struct trace_event_raw_sys_enter *)ctx)->args[0], (__s64)((struct trace_event_raw_sys_enter *)ctx)->args[1]);
    return 0;
}
