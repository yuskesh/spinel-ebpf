// SPDX-License-Identifier: GPL-2.0 OR MIT
//
// GENERATED by spinel-ebpf. Do not edit by hand.
// Source unit: 14_calls.rb
// ebpf-eligible methods: 3, classes touched: 0

#include "vmlinux.h"
#include <bpf/bpf_helpers.h>

char LICENSE[] SEC("license") = "Dual MIT/GPL";

/* ctx for twice — userspace fills before bpf_prog_test_run */
struct twice_ctx {
    __s64 x;
};

/* ctx for quad — userspace fills before bpf_prog_test_run */
struct quad_ctx {
    __s64 x;
};

/* ctx for six_times — userspace fills before bpf_prog_test_run */
struct six_times_ctx {
    __s64 x;
};

/* impl: twice : int  params: x: int */
static __noinline __s64 twice_inner(__s64 x)
{
    return x * 2;
}

/* entry wrapper: twice */
SEC("syscall")
int twice(struct twice_ctx *ctx)
{
    return (int)twice_inner(ctx->x);
}

/* impl: quad : int  params: x: int */
static __noinline __s64 quad_inner(__s64 x)
{
    return twice_inner(x) + twice_inner(x);
}

/* entry wrapper: quad */
SEC("syscall")
int quad(struct quad_ctx *ctx)
{
    return (int)quad_inner(ctx->x);
}

/* impl: six_times : int  params: x: int */
static __noinline __s64 six_times_inner(__s64 x)
{
    return twice_inner(x) + quad_inner(x);
}

/* entry wrapper: six_times */
SEC("syscall")
int six_times(struct six_times_ctx *ctx)
{
    return (int)six_times_inner(ctx->x);
}
