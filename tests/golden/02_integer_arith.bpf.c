// SPDX-License-Identifier: GPL-2.0 OR MIT
//
// GENERATED by spinel-ebpf. Do not edit by hand.
// Source unit: 02_integer_arith.rb
// ebpf-eligible methods: 2, classes touched: 0

#include "vmlinux.h"
#include <bpf/bpf_helpers.h>

char LICENSE[] SEC("license") = "Dual MIT/GPL";

/* ctx for add — userspace fills before bpf_prog_test_run */
struct add_ctx {
    __s64 a;
    __s64 b;
};

/* ctx for sub — userspace fills before bpf_prog_test_run */
struct sub_ctx {
    __s64 a;
    __s64 b;
};

/* impl: add : int  params: a: int, b: int */
static __noinline __s64 add_inner(__s64 a, __s64 b)
{
    return a + b;
}

/* entry wrapper: add */
SEC("syscall")
int add(struct add_ctx *ctx)
{
    return (int)add_inner(ctx->a, ctx->b);
}

/* impl: sub : int  params: a: int, b: int */
static __noinline __s64 sub_inner(__s64 a, __s64 b)
{
    return a - b;
}

/* entry wrapper: sub */
SEC("syscall")
int sub(struct sub_ctx *ctx)
{
    return (int)sub_inner(ctx->a, ctx->b);
}
