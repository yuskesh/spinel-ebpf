// SPDX-License-Identifier: GPL-2.0 OR MIT
//
// GENERATED by spinel-ebpf. Do not edit by hand.
// Source unit: 59_task_storage.rb
// ebpf-eligible methods: 1, classes touched: 0

#include "vmlinux.h"
#include <bpf/bpf_helpers.h>
#include <bpf/bpf_tracing.h>
char LICENSE[] SEC("license") = "Dual MIT/GPL";

/* top-level ivar @max : int */
struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __type(key, __u32);
    __type(value, __s64);
    __uint(max_entries, 1);
} u_59_task_storage_top_max SEC(".maps");

/* per-task local storage. task_store(v) / task_load() read+write
 * a value scoped to the current task_struct (no explicit key). */
struct {
    __uint(type, BPF_MAP_TYPE_TASK_STORAGE);
    __uint(map_flags, BPF_F_NO_PREALLOC);
    __type(key, int);
    __type(value, __s64);
} bpf_task_store SEC(".maps");

/* __always_inline: the PTR_TRUSTED task from bpf_get_current_task_btf()
 * must reach bpf_task_storage_get in the program's own context, not
 * across a __noinline sub-prog boundary (where it degrades and the
 * storage silently fails to persist). */
static __always_inline __s64 spnl_task_load(void)
{
    struct task_struct *t = bpf_get_current_task_btf();
    __s64 *v = bpf_task_storage_get(&bpf_task_store, t, 0, 0);
    return v ? *v : 0;
}

static __always_inline __s64 spnl_task_store(__s64 value)
{
    struct task_struct *t = bpf_get_current_task_btf();
    __s64 *v = bpf_task_storage_get(&bpf_task_store, t, 0, BPF_LOCAL_STORAGE_GET_F_CREATE);
    if (v) { *v = value; }
    return value;
}

static __always_inline __s64 spnl_task_incr(__s64 delta)
{
    struct task_struct *t = bpf_get_current_task_btf();
    __s64 *v = bpf_task_storage_get(&bpf_task_store, t, 0, BPF_LOCAL_STORAGE_GET_F_CREATE);
    if (!v) return 0;
    *v += delta;
    return *v;
}

/* single-get read-modify-write - store `value`, return the prior
 * value. One bpf_task_storage_get so it stays clear of the two-get
 * aliasing quirk. */
static __always_inline __s64 spnl_task_swap(__s64 value)
{
    struct task_struct *t = bpf_get_current_task_btf();
    __s64 *v = bpf_task_storage_get(&bpf_task_store, t, 0, BPF_LOCAL_STORAGE_GET_F_CREATE);
    if (!v) return 0;
    __s64 old = *v;
    *v = value;
    return old;
}

/* ctx for kprobe__do_sys_openat2 — userspace fills before bpf_prog_test_run */
struct kprobe__do_sys_openat2_ctx {
    __s64 dfd;
};

/* impl: kprobe__do_sys_openat2 : int  params: dfd: int */
static __noinline __s64 kprobe__do_sys_openat2_inner(__s64 dfd)
{
    __s64 n = 0;
    n = spnl_task_incr(1);
    __u32 _k1 = 0;
    __s64 *_p2 = bpf_map_lookup_elem(&u_59_task_storage_top_max, &_k1);
    __s64 _if3 = 0;
    if (n > (_p2 ? *_p2 : 0)) {
        __u32 _k4 = 0;
        __s64 _v5 = n;
        bpf_map_update_elem(&u_59_task_storage_top_max, &_k4, &_v5, BPF_ANY);
        _if3 = _v5;
    }
    return n;
}

/* entry wrapper: kprobe__do_sys_openat2 [kprobe -> kprobe/do_sys_openat2] */
SEC("kprobe/do_sys_openat2")
int kprobe__do_sys_openat2(struct pt_regs *ctx)
{
    (void)ctx;
    (void)kprobe__do_sys_openat2_inner((__s64)PT_REGS_PARM1(ctx));
    return 0;
}
