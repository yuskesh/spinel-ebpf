# NOTE: .ast files used to be excluded, but the AST parser (parse_spinel_ast.rb)
# also needs them as round-trip fixtures. Both .ast and .ir are now committed
# and regenerated by scripts/regen-fixtures.sh.
